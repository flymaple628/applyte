`<%# program %>

<div class="search-result-item">
  <div class="search-result-item-index"> <%= program_counter+1 %> </div>
	<div class="search-result-item-title"> <%= program.school.name %> </div>
  <div class="search-result-item-logo"></div>
	<div class="search-result-item-desciption">
    <div class="title"> <%= program.school.name %> </div>
		<div class="content">
			<%= program.desc %>
		</div>
	</div>
	<div class="search-result-item-add">
		<% if check_user_program_forms(program) %>
			<%= link_to "Add", fevorite_myprogram_path(program), :class=>"add-fav",:data => { :row_id => program.id },:remote=>true%>
		<% else %>
			<%= link_to "Add", fevorite_myprogram_path(program), :class=>"add-fav",:data => { :row_id => program.id },:remote=>true%>
		<% end %>
	</div>
</div>

